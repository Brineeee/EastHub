local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/synolope/meepcracked/main/ui-engine.lua"))()

local UserText = {
    main_color = Color3.fromRGB(0,0,255), 
    min_size = Vector2.new(300, 350),
    toggle_key = Enum.KeyCode.Unknown,
    can_resize = true,
}
local Window = library:AddWindow("East Hub | Game Tab", UserText)
local Welcome = Window:AddTab("Games")

Welcome:AddButton("Blox Fruit ~ Supported", function()
wait(1)
local imgui = game:GetService("CoreGui"):FindFirstChild("imgui2")
	if imgui then imgui:Destroy()
end
wait(2)
loadstring(game:HttpGet("https://raw.githubusercontent.com/yooshit763/SHAREHACK-FREE/main/BloxFruit"))()
end) 

Welcome:AddButton("Pet Simulator X ~ Supported", function()
wait(1)
local imgui = game:GetService("CoreGui"):FindFirstChild("imgui2")
	if imgui then imgui:Destroy()
end
wait(2)
loadstring(game:HttpGet("https://raw.githubusercontent.com/yooshit763/FreeScripts/main/PetSimulatorX"))()
end) 

Welcome:AddButton("Natural Disaster Survival ~ Supported", function()
wait(1)
local imgui = game:GetService("CoreGui"):FindFirstChild("imgui2")
	if imgui then imgui:Destroy()
end
wait(2)
loadstring(game:HttpGet("https://raw.githubusercontent.com/yooshit763/FreeScripts/main/NaturalDisaster"))()
end) 

Welcome:AddButton("Legends Of Speed ~ Supported", function()
wait(1)
local imgui = game:GetService("CoreGui"):FindFirstChild("imgui2")
	if imgui then imgui:Destroy()
end
wait(2)
loadstring(game:HttpGet("https://raw.githubusercontent.com/yooshit763/FreeScripts/main/LegendsOfSpeed"))()
end) 

Welcome:Show() 
library:FormatWindows()
